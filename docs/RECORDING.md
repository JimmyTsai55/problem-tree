# Recording demo.gif

1. Split screen: Claude Code on the left, Cursor Markdown preview (Mermaid extension) of `docs/superpowers/handoff/<topic>.md` on the right.
2. Script (~20s): `加子問題 A.3：…` → `進 A.3` → `A.3 解決了：…`. Node goes uncolored → 🔶 yellow → ✅ green, status line updates.
3. Record with `Cmd+Shift+5` (macOS) or Kap; export GIF ≤ 5 MB, ~1200px wide.
4. Save as `docs/demo.gif`, uncomment the image line in README.md, delete the TODO comment.
